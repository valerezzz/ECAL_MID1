// creation d'une constante globale
const constante = `ECAL`;
// creation d'une variable globale
let variable = 1;

// definition de la fonction start
function start() {
  // faire des logs dans la console
  console.log("Ceci est ma constante : ", constante);

  console.log("Ceci est ma variable:", variable);

  console.log(`Ceci est ma variable modifiée: MID ${variable} 2023`);
}
// appel de la fonction start
start();
