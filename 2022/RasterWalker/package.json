{
  "dependencies": {
    "@onemorestudio/playgroundjs": "^1.0.0",
    "babel-plugin-transform-remove-console": "^6.9.4",
    "file-system": "^2.2.2",
    "parcel": "*",
    "parcel-reporter-static-files-copy": "^1.3.0",
    "pixi.js": "^6.3.0"
  },
  "name": "RasterWalker",
  "version": "1.0.0",
  "devDependencies": {
    "stream-browserify": "^3.0.0"
  },
  "scripts": {
    "start": "rm -rf dist && parcel index.html --port 80 --open",
    "start-https": "rm -rf dist && parcel index.html --cert certificate.cert --key certificate.key --port 80 --open",
    "build": "rm -rf dist && parcel build index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
